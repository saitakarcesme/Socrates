{
  "name": "@socrates/runner-local",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint src",
    "test:native": "tsx src/oci/native-validation.ts",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@socrates/contracts": "workspace:*"
  },
  "devDependencies": {
    "@socrates/database": "workspace:*",
    "tsx": "^4.23.1",
    "vitest": "^3.2.7"
  }
}
