#include <tunables/global>

profile socrates-sandbox flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>

  file,
  signal,

  deny /tmp/socrates-lsm-probe w,
  deny mount,
  deny umount,
  deny pivot_root,
  deny ptrace,
}
